export const UI={showToast(message,type='success'){const toastEl=document.getElementById('global-toast');if(!toastEl)return;toastEl.textContent=message;toastEl.className=`toast show bg-${type}text-white`;setTimeout(()=>{toastEl.classList.remove('show');},3000);}};window.UI=UI;window.showToast=(message,type)=>UI.showToast(message,type);